# Nginx Proxy Manager includes this file inside every proxy server block.
# Keep one canonical public host while preserving the path and query string.
if ($host = www.paratunisie.com) {
  return 301 https://paratunisie.com$request_uri;
}
