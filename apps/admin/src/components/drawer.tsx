"use client";

// Namespace shim — the shared Drawer lives in @paratunisie/ui (SPRINT A).
// Kept so existing imports (`@/components/drawer`) keep working; new code
// should import from "@paratunisie/ui" directly.
export { Drawer } from "@paratunisie/ui";
